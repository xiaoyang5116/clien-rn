import React from 'react';
import MapView from '../../components/map';

const TownTabPage = (props) => {
    return (
        <MapView>
            
        </MapView>
    );
}

export default TownTabPage;

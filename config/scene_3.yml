scenes: [
  # 破庙门口场景
  {
    id: 'pomiaomk', name: '破庙门口',

    # 进入场景事件处理动作ID列表
    enter_actions: ['a0'],

    # 变量定义
    vars: [
    ],

    # 动作定义
    actions: [
      { id: 'a0', cmd: 'chat', params: 'c1' },
      { id: 'b1', cmd: 'scene', params: 'pomiao' },
      { id: 'b2', cmd: 'scene', params: 'hutongkou' },
    ],

    # 对话选项
    chats: [
      #
      { 
        id: 'c1', desc: '请选择移动', 
        options: [ 
          { title: '【进入破庙】', click_actions: ['b1'] }, 
          { title: '【落月镇西-胡同口】', click_actions: ['b2'] }, 
        ],
      }
    ],

    # 旁白
    asides: [
    ],

    # 对话框
    dialogs: [
    ]
  },
]